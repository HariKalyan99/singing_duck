{
  "name": "singing-duck-monorepo",
  "private": true,
  "description": "Singing Duck app + @singing-duck/capture-duck package (see packages/capture-duck)",
  "scripts": {
    "test:capture-duck": "npm test -w @singing-duck/capture-duck 2>/dev/null || npm test --prefix packages/capture-duck",
    "trial:consumer": "npm install --prefix examples/trial-consumer && node examples/trial-consumer/node-trial.mjs"
  }
}
